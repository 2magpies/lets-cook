import React from 'react';

// Hou comment: we could clean up the HTML tags here
function Header() {
  return (
    <div className="banner">
      <h1> Let's Cook!</h1>
    </div>
  );
}

export default Header;
